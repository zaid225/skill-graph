# Base URL of the deployed Worker API. In local dev, leave unset and rely on
# the Vite proxy in vite.config.ts pointing at `wrangler dev`.
VITE_API_BASE_URL=https://skillgraph-backend.<your-subdomain>.workers.dev
